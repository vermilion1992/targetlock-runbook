"use client";

/**
 * @deprecated Use TrajectoryWorkspace from `./trajectory-workspace`.
 * Kept so existing import paths do not break during the cockpit overhaul.
 */
export { TrajectoryWorkspace as TrajectoryGraphicsViewer } from "./trajectory-workspace";
