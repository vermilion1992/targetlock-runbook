"use client"
import React from "react";
import Lottie from "lottie-react";

const loginAnimation = () => <Lottie animationData={'/public/animation/login.json'} loop={true} />;

export default loginAnimation;